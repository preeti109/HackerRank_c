#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    int a, b, c, d;
    scanf("%d %d", &a, &b);
    c=a>b?a:b;
    d=a<b?a:b;
    for(d;d<=c;d++){
        if(d<10){
            if(d==1)
            printf("one\n");
            if(d==2)
            printf("two\n");
            if(d==3)
            printf("three\n");
            if(d==4)
            printf("four\n");
            if(d==5)
            printf("five\n");
            if(d==6)
            printf("six\n");
            if(d==7)
            printf("seven\n");
            if(d==8)
            printf("eight\n");
            if(d==9)
            printf("nine\n");
        }
        if(d>=10){
            if(d%2==0)
                printf("even\n");
            else
                printf("odd\n");
        }
    }
    return 0;
}
